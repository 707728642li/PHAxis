"""Audited third-party fallbacks shipped inside the PHAxis wheel.

Vendored components are used only when the corresponding standard-library
implementation is unavailable, including the source verifier's intentional
``python -S`` no-site probe on Python 3.10.
"""

from __future__ import annotations
