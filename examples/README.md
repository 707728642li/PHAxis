# Example

`phaxis demo --output demo-results` generates synthetic geometry, runs the installed fusion/trait exporter and writes an offline report. No external model/image access occurs. Golden tests cover two-hair and zero-hair cases.
